# -*- coding: UTF-8 -*-
from django.conf.urls.defaults import *

urlpatterns = patterns('newtype',
    # Example:
    # (r'^newtype/', include('newtype.foo.urls')),
    (r'^getall/$','chart.views.getall'),
)
